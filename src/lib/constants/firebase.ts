export const collections = {
  posts: 'posts',
  categories: 'categories',
  blogs: 'blogs',
} as const;

export const bucketPaths = {
  blogs: 'blogs',
  posts: 'posts',
  users: 'users',
} as const;
